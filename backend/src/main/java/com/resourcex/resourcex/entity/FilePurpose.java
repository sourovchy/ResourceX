package com.resourcex.resourcex.entity;

/**
 * What an uploaded file is used for. ResourceX only stores image references,
 * so the set is intentionally small.
 */
public enum FilePurpose {
    AVATAR,
    ITEM_IMAGE,
    STUDENT_ID
}
